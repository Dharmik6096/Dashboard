"use client";
import { useState } from "react";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import api from "@/lib/api";
export function ContactForm(){const[state,setState]=useState<"idle"|"sending"|"sent"|"error">("idle");const[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");setError("");const form=new FormData(e.currentTarget);try{await api.post("/public/contact",Object.fromEntries(form.entries()));setState("sent");e.currentTarget.reset()}catch(err:unknown){const detail=(err as {response?:{data?:{detail?:string}}}).response?.data?.detail;setError(detail||"We could not send your request. Please try again.");setState("error")}}if(state==="sent")return <div className="info-card" style={{textAlign:"center",padding:48}}><span className="info-card-icon" style={{margin:"0 auto 20px",color:"#168b68",background:"#e6f7f1"}}><CheckCircle2 size={22}/></span><h3>Message received</h3><p>Our team will respond using the email address you provided.</p><button className="btn btn-secondary" style={{marginTop:20}} onClick={()=>setState("idle")}>Send another message</button></div>;return <form className="contact-form" onSubmit={submit}><div className="contact-row"><label>First name<input required name="first_name" autoComplete="given-name"/></label><label>Last name<input required name="last_name" autoComplete="family-name"/></label></div><label>Work email<input required type="email" name="email" autoComplete="email"/></label><label>Company<input required name="company" autoComplete="organization"/></label><label>How can we help?<select name="topic" defaultValue="demo"><option value="demo">Product demo</option><option value="pricing">Pricing and procurement</option><option value="security">Security review</option><option value="support">Technical support</option><option value="careers">Careers</option></select></label><label>Details<textarea required name="message" rows={5} placeholder="Tell us about your infrastructure and what you want to improve."/></label>{state==="error"&&<p className="form-error">{error}</p>}<button disabled={state==="sending"} className="btn btn-primary btn-lg" type="submit">{state==="sending"?"Sending…":"Send request"}<ArrowRight size={16}/></button></form>}

